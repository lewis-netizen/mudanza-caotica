-- ObjectDefinition — sofá mediano (GAM-001).
return {
    ObjectId = "sofa_medium",
    Size = "medium",
    Properties = {
        -- Multiplicador de WalkSpeed mientras se carga (0–1). GAM-005.
        carrySpeedMultiplier = 0.6,
    },
}
