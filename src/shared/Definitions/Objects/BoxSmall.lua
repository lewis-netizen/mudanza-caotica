-- ObjectDefinition — caja pequeña (GAM-001).
-- Identidad y configuración. La apariencia placeholder viene de
-- GameplayConfig.PLACEHOLDER_OBJECT_SIZES — nunca de esta definición.
return {
    ObjectId = "box_small",
    Size = "small",
    Properties = {},
}
