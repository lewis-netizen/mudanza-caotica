# deferrals.txt — diferimientos AUTORIZADOS y ACOTADOS de deuda de derivación.
# Reemplaza a baseline.txt (DL-050). Formato:
#   <ticket> <dl> <autorizado-por> <hasta YYYY-MM-DD>
# Un stale con fila vigente = deuda diferida (no bloquea).
# Un stale con fila VENCIDA = violación (la obligación de liveness expiró).
# Un stale sin fila = violación (regresión).
# Reconciliar (el ticket referencia el DL en su "Deriva de") elimina la fila.
# Bound inicial: 2026-08-11 (deadline del vertical slice, DL-024) — el PO
# puede apretar o extender fechas; extender es una decisión, no un default.
